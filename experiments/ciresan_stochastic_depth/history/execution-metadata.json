{
  "source": "Selected W&B public run requirements.txt files; read-only inspection of output.log and artifact-index manifests for provenance gaps",
  "sanitization": "Whitelisted program basename, public scientific runtime/hardware metadata, git commit, package versions. No full downloaded file content retained.",
  "runs": [
    {
      "id": "ts4k9n55",
      "requirements": {
        "numpy": "numpy==1.23.5",
        "scipy": "scipy==1.11.4",
        "torch": "torch==2.1.0+cu121",
        "torchvision": "torchvision==0.16.0+cu121",
        "wandb": "wandb==0.16.3"
      },
      "runtime_environment_metadata_available": false
    },
    {
      "id": "s0og2713",
      "requirements": {
        "numpy": "numpy==1.15.4",
        "scipy": "scipy==1.1.0",
        "torch": "torch==1.1.0",
        "torchvision": "torchvision==0.2.2",
        "wandb": "wandb==0.8.12"
      },
      "runtime_environment_metadata_available": false
    },
    {
      "id": "0ovwv4x6",
      "requirements": {
        "numpy": "numpy==1.15.4",
        "scipy": "scipy==1.1.0",
        "torch": "torch==1.3.1",
        "torchvision": "torchvision==0.4.2",
        "wandb": "wandb==0.8.15"
      },
      "runtime_environment_metadata_available": false
    },
    {
      "id": "425pu650",
      "requirements": {
        "numpy": "numpy==1.15.4",
        "scipy": "scipy==1.1.0",
        "torch": "torch==1.1.0",
        "torchvision": "torchvision==0.2.2",
        "wandb": "wandb==0.8.12"
      },
      "runtime_environment_metadata_available": false
    }
  ],
  "output_log_provenance": [
    {
      "id": "ts4k9n55",
      "output_log_source_basenames": [],
      "interpretation": "Mentions in output.log are supporting provenance only; presence alone does not prove this is the entire executed program."
    },
    {
      "id": "s0og2713",
      "output_log_source_basenames": [],
      "interpretation": "Mentions in output.log are supporting provenance only; presence alone does not prove this is the entire executed program."
    },
    {
      "id": "0ovwv4x6",
      "output_log_source_basenames": [],
      "interpretation": "Mentions in output.log are supporting provenance only; presence alone does not prove this is the entire executed program."
    },
    {
      "id": "425pu650",
      "output_log_source_basenames": [],
      "interpretation": "Mentions in output.log are supporting provenance only; presence alone does not prove this is the entire executed program."
    }
  ],
  "interpretation": "Selected package versions available; wandb-metadata.json absent from run files. Artifact manifests were found but contain parquet indices, not program/hardware provenance. Executed architecture/program and hardware remain unverified where no independent source record is available."
}
